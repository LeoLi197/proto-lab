# backend/.gcloudignore
# ------------------------------------------------------------------------------
# 此文件用于精确控制 `gcloud run deploy --source` 命令打包上传的文件。
#
# 通过创建此文件，我们覆盖了 gcloud 的默认行为，改为一个更可控的策略：
# "默认包含所有文件，只排除下面明确列出的内容"。
# 这样可以确保所有需要的文件都被正确包含。
# ------------------------------------------------------------------------------

# 忽略 gcloudignore 文件本身和 Git 相关文件
.gcloudignore
.git
.gitignore

# 忽略 Python 虚拟环境目录（如果在 backend 目录中创建了的话）
/env/
/venv/
.venv/

# 忽略 Python 缓存文件
__pycache__/
*.pyc